---
import Base from '../layouts/Base.astro';
---
<Base
  title="Tenure | AI Proxy for memory and agent enforcement"
  description="Tenure gives every AI surface access to institutional knowledge, turns observed agent behavior into policy, enforcing not just what's allowed, but what must be true before it happens."
>
  <main>

    <section id="hero">
      <div class="container">
        <div class="hero-badge">
          <span class="hero-badge-dot"></span>
          No SDKs · Out-of-model Security · Zero Telemetry
        </div>


        <h1>The model does the thinking. </br> <span>Tenure</span> does the policing. </h1>


          <p class="hero-rag-line">Asking models to follow fragile system prompts is a losing battle.</p>

        <p class="hero-sub hero-sub-lede">

        Tenure gives every chat client, IDE, and agent access to institutional knowledge, turns observed agent behavior into policy, enforcing not just what's allowed, but what must be true before it happens.

        </p>

        <div class="hero-cta-row">
          <a href="/how-it-works" class="btn-primary">See How It Works</a>
          <a href="/pricing" class="btn-secondary">View Pricing</a>
        </div>

         <div class="hero-stats">
          <div class="hero-stat">
            <span class="hero-stat-value">1.0<a href="https://huggingface.co/spaces/tenurehq/precisionmembench" style="color:var(--accent);font-size:0.7em;vertical-align:super;text-decoration:none">*</a></span>
            <span class="hero-stat-label">Retrieval precision</span>
          </div>
          <div class="hero-stat">
            <span class="hero-stat-value">&lt;15ms</span>
            <span class="hero-stat-label">latency</span>
          </div>
          <div class="hero-stat">
            <span class="hero-stat-value">0.00</span>
            <span class="hero-stat-label">Drift score</span>
          </div>
          <div class="hero-stat">
            <span class="hero-stat-value">0</span>
            <span class="hero-stat-label">SDK dependencies</span>
          </div>
        </div>

        <div class="gateway-mock" aria-label="Example runtime decision made by Tenure">
          <div class="gateway-mock-bar">
            <span class="term-dot term-dot-red"></span>
            <span class="term-dot term-dot-yellow"></span>
            <span class="term-dot term-dot-green"></span>
            <span class="gateway-mock-title">RUNTIME GATEWAY <span class="gateway-mock-agent">agent:sales-outbound</span></span>
          </div>

          <div class="gateway-mock-body">
            <div class="gateway-mock-intent">
              <span class="gateway-mock-eyebrow">Agent Intent</span>
              <span class="gateway-mock-quote">&ldquo;Send follow-up offer to Prospect&rdquo;</span>
            </div>

            <div class="gateway-mock-eyebrow gateway-mock-eyebrow-checks">Evaluating preconditions&hellip;</div>

            <ul class="gateway-checks">
              <li class="gateway-check gateway-check-pass" style="--d:1">
                <span class="gateway-check-name">Identity <span class="gateway-check-dim">&mdash; Token authenticated, workload-id 9482</span></span>
                <span class="gateway-check-badge">PASS</span>
              </li>
              <li class="gateway-check gateway-check-pass" style="--d:2">
                <span class="gateway-check-name">Auth <span class="gateway-check-dim">&mdash; CRM write permission</span></span>
                <span class="gateway-check-badge">PASS</span>
              </li>
              <li class="gateway-check gateway-check-fail" style="--d:3">
                <span class="gateway-check-name">Precondition <span class="gateway-check-dim">&mdash; &ldquo;not currently a paying user&rdquo;</span></span>
                <span class="gateway-check-badge">FAIL</span>
                <span class="gateway-check-evidence">Evidence: CRM query returned &lsquo;active&rsquo;</span>
              </li>
            </ul>

            <div class="gateway-decision" style="--d:4">
              <span class="gateway-decision-label">Decision</span>
              <span class="gateway-decision-value">Block &amp; escalate</span>
              <span class="gateway-decision-rule">Policy #104</span>
            </div>
          </div>
        </div>

      </div>
    </section>

    <section id="state-vs-context">
      <div class="container">
        <span class="label">The problem</span>
        <h2>Your AI can act. Your infrastructure can't judge when it should.</h2>
        <p>
        AI is no longer trapped inside a chat bubble. Whether it's an internal copilot querying databases, a background agent updating a CRM, or a model executing tool calls, downstream business systems only check the API token. They are blind to whether the agent's actual intent is safe, authorized, or completely rogue.
        </p>

        <div class="problem-grid problem-grid-strong">
          <article class="problem-card">
            <span class="problem-number">01</span>
            <h3>Authenticated does not mean appropriate.</h3>
            <p>A valid token proves who is calling. It does not stop a customer-support bot from calling an internal deletion API.</p>
          </article>
          <article class="problem-card">
            <span class="problem-number">02</span>
            <h3>Permission does not equal intent.</h3>
            <p>An agent may have CRM access without being allowed to update every customer in every situation.</p>
          </article>
          <article class="problem-card">
            <span class="problem-number">03</span>
            <h3>Observability happens too late.</h3>
            <p>Logs will tell you how you got hacked. Enforcement stops it from happening.</p>
          </article>
        </div>
      </div>
    </section>

    <section id="architecture" class="architecture-section">
      <div class="container">
        <span class="label">Where Tenure fits</span>
        <h2>One gateway every agent request passes through.</h2>
        <p>
        Tenure provides the right institutional knowledge and evaluates policy before allowing, blocking, or escalating an action in under 15ms.
        </p>

        <div class="architecture-shell" aria-label="Tenure architecture diagram">
          <div class="architecture-row architecture-row-top">
            <div class="architecture-node client-node">Chat client</div>
            <div class="architecture-node client-node">IDE client</div>
            <div class="architecture-node client-node">Interactive agent</div>
            <div class="architecture-node client-node">Background agent</div>
          </div>

          <div class="architecture-arrow-row">
            <span></span><span></span><span></span><span></span>
          </div>

          <div class="architecture-gateway">
            <span class="beta-badge">Private beta</span>
            <div class="architecture-gateway-header">
              <span class="architecture-kicker">Tenure</span>
              <h3>Runtime gateway</h3>
              <p>Every request gets eligible context. Every consequential action is checked before it executes.</p>
            </div>

            <div class="architecture-controls architecture-controls-simple">
              <div class="architecture-control">
                <strong>Identity</strong>
                <span>Every agent authenticates before it acts</span>
              </div>
              <div class="architecture-control">
                <strong>Permissions &amp; evidence</strong>
                <span>Checked against what the task actually requires</span>
              </div>
              <div class="architecture-control">
                <strong>Decision</strong>
                <span>Allow, block, or escalate, logged every time</span>
              </div>
            </div>
          </div>

          <div class="architecture-arrow-row architecture-arrow-row-bottom">
            <span></span><span></span><span></span><span></span>
          </div>

          <div class="architecture-row architecture-row-bottom-models">
            <div class="architecture-node model-node">Models</div>
            <div class="architecture-node model-node">Tools</div>
            <div class="architecture-node model-node">APIs</div>
            <div class="architecture-node model-node">Business Systems</div>
          </div>
        </div>
       
      </div>
    </section>

    <section id="deterministic-policy" class="section-surface" style="border-bottom: 1px solid var(--border2, #2a2a2a);">
      <div class="container">
        <span class="label" style="color: var(--accent);">Deterministic Mapping</span>
        <h2>No "AI magic." Just absolute wire precision.</h2>
        <p class="section-lede" style="margin-bottom: 3rem;">
          When security vendors promise <q>automated policy generation,</q> engineers expect an LLM wrapper that will hallucinate tool names, mangle argument schemas, and force you into endless cycles of manual configuration editing. 
        </p>

        <div class="wire-parse-mock" aria-label="Visual demonstration of wire parsing vs guessing">
          <div class="wire-panel">
            <div class="wire-panel-header">RAW WIRE PAYLOAD (Deterministic Input)</div>
            <pre><code is:raw>{
  "type": "tool_call",
  "name": "stripe_refund_customer",
  "arguments": {
    "user_id": "usr_9482",
    "amount_cents": 5000
  }
}</code></pre>
          </div>
          
          <div class="wire-arrow-connector">➔</div>

          <div class="wire-panel panel-highlight">
            <div class="wire-panel-header" style="color: var(--accent);">OBSERVED CAPABILITY MATRIX</div>
            <div class="wire-matrix-row">
              <input type="checkbox" checked disabled />
              <span>Capability: <code>stripe_refund_customer</code></span>
            </div>
            <div class="wire-matrix-row">
              <input type="checkbox" checked disabled />
              <span>Argument constraint: <code>user_id</code> (String)</span>
            </div>
            <div class="wire-matrix-row highlight-row">
              <input type="checkbox" checked style="accent-color: var(--accent);" />
              <span>Argument constraint: <code>amount_cents</code> (Integer)</span>
            </div>
            <div class="wire-caption"><small>If Tenure didn't observe it, it isn't on this list. No freeform entry, no guessing at what your agent might call.</small></div>
          </div>
        </div>

      </div>
    </section>


    <section id="observe-modes" class="architecture-section">

    <div class="container">
        <span class="label">Observation Mode</span>
        <h2>Two ways to observe</h2>
        <p>
        Policy can only contain what Tenure has observed, but observation itself looks different depending on where the agent is.
        </p>

        <p>

        If an agent is completely unconstrained in production, observation alone will not stop it from causing harm before a policy exists. Tenure therefore supports the same discovery model at two different stages in an agent's lifecycle:
        </p>


         <div class="lifecycle-tracks" style="margin: 2rem 0; display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem;">
      <div style="background: var(--surface2); border: 1px solid var(--border); padding: 1.5rem; border-radius: 10px;">
        <h3 style="margin-top:0; font-size: 1.1rem; color: var(--text);">1. In Production: Track the Baseline</h3>
        <p style="font-size: 0.88rem; margin-bottom:0;">
          Most agents worth worrying about are already live: calling <code>fetch_customer</code>, drafting emails, touching real infrastructure. Tenure observes those workflows as they run and captures what the agent actually does. 
        </p>
      </div>
      <div style="background: var(--surface2); border: 1px solid var(--border-accent || var(--border)); padding: 1.5rem; border-radius: 10px; box-shadow: 0 0 15px rgba(0,168,173,0.02);">
        <h3 style="margin-top:0; font-size: 1.1rem; color: var(--accent);">2. In Development: GroundEval</h3>
        <p style="font-size: 0.88rem; margin-bottom:0;">
        If the agent isn't live yet, or you're reshaping its environment, you don't have to guess blind. <b><a href="https://github.com/tenurehq/GroundEval">GroundEval</a></b> is our free tool for the development stage. Point it at your agent in staging, and its Observe Mode drafts a policy from a real test run.
        </p>
      </div>
    </div>

    </div>

    </section>

    <section id="scoped-memory" class="section-surface">
      <div class="container">
        <span class="label">Shared scoped memory</span>
        <h2>Shared memory without shared everything.</h2>
        <p class="section-lede">
          Tenure preserves your organization’s institutional knowledge across every AI tool, without mixing teams, projects, or users into one shared pool.
        </p>

        <div class="memory-visual">
          <div class="memory-scope-stack">
            <div class="memory-scope scope-org">
              <span class="scope-kind">ORG</span>
              <strong>Never send customer secrets to third-party tools.</strong>
              <small>Absolute organizational rule</small>
            </div>
            <div class="memory-scope scope-team">
              <span class="scope-kind">TEAM</span>
              <strong>Platform APIs use structured error envelopes.</strong>
              <small>Shared team practice</small>
            </div>
            <div class="memory-scope scope-project">
              <span class="scope-kind">PROJECT</span>
              <strong>Payments service migration targets PostgreSQL 18.</strong>
              <small>Project-specific state</small>
            </div>
            <div class="memory-scope scope-user muted-scope">
              <span class="scope-kind">USER</span>
              <strong>Prefer concise explanations with code first.</strong>
              <small>Persona lane · not eligible for this token</small>
            </div>
          </div>

          <div class="memory-token-card">
            <div class="memory-token-header">
              <span>Token policy</span>
              <strong>agent:code-reviewer</strong>
            </div>
            <div class="token-policy-row"><span>Org context</span><b class="policy-on">Allowed</b></div>
            <div class="token-policy-row"><span>Team: platform</span><b class="policy-on">Allowed</b></div>
            <div class="token-policy-row"><span>Project: payments</span><b class="policy-on">Allowed</b></div>
            <div class="token-policy-row"><span>User persona</span><b class="policy-off">Off</b></div>
            <div class="token-policy-divider"></div>
            <div class="token-output">
              <span class="token-output-label">Eligible context</span>
              <strong>3 scoped beliefs</strong>
              <small>Only active, relevant context can enter the request.</small>
            </div>
          </div>
        </div>

        <div class="three-pillars">
          <div class="pillar">
            <div class="pillar-label">Memory built for models, not humans</div>
            <p>
            Tenure structures the decisions, practices, relationships, preferences, and corrections that run your org into beliefs the model understands.</p>
          </div>
          <div class="pillar">
            <div class="pillar-label">Cryptographic Isolation</div>
            <p>Every chat client, IDE, and agent is only given access to the knowledge it's allowed to. No worrying about context bleed across projects.</p>
          </div>
          <div class="pillar">
            <div class="pillar-label">Tamper-Proof Audit Trails</div>
            <p>Every belief retains its origin, scope, and lifecycle history. Know why an agent knew something, and when it found out.</p>
          </div>
        </div>
      </div>
    </section>

    <section id="precision" class="section-surface">
      <div class="container">
        <span class="label">Memory precision</span>
        <h2>The right belief, at the right time. Nothing extra.</h2>
        <p class="section-lede">
        Other memory systems drag loosely related memories forward as the conversation changes. Tenure provides the right belief at each turn, so when the topic shifts, irrelevant context stays behind.
        </p>

        <div class="precision-proof">
          <div class="precision-chart">
            <div class="bar-row">
              <span class="bar-label tenure-label">Tenure</span>
              <div class="bar-track"><div class="bar-fill tenure-fill"></div></div>
              <span class="bar-value tenure-val">1.00</span>
            </div>
            <div class="bar-row">
              <span class="bar-label">Open Knowledge Format</span>
              <div class="bar-track"><div class="bar-fill comparison-fill" style="width:47%"></div></div>
              <span class="bar-value other-val">0.47</span>
            </div>
            <div class="bar-row">
              <span class="bar-label">Supermemory</span>
              <div class="bar-track"><div class="bar-fill comparison-fill" style="width:22%"></div></div>
              <span class="bar-value other-val">0.22</span>
            </div>
             <div class="bar-row">
              <span class="bar-label">gbrain</span>
              <div class="bar-track"><div class="bar-fill comparison-fill" style="width:14%"></div></div>
              <span class="bar-value other-val">0.14</span>
            </div>
            <div class="bar-row">
              <span class="bar-label">Zep</span>
              <div class="bar-track"><div class="bar-fill comparison-fill" style="width:9%"></div></div>
              <span class="bar-value other-val">0.09</span>
            </div>
            <div class="bar-row">
              <span class="bar-label">Mem0</span>
              <div class="bar-track"><div class="bar-fill comparison-fill" style="width:6%"></div></div>
              <span class="bar-value other-val">0.06</span>
            </div>
            <div class="bar-row">
              <span class="bar-label">Hindsight</span>
              <div class="bar-track"><div class="bar-fill comparison-fill" style="width:6%"></div></div>
              <span class="bar-value other-val">0.06</span>
            </div>
          </div>

          <div class="precision-stat-stack">
            <div class="precision-stat">
              <strong>89</strong>
              <span>benchmark cases</span>
            </div>
            <div class="precision-stat">
              <strong>&lt;15ms</strong>
              <span>context injection</span>
            </div>
            <div class="precision-stat">
              <strong>0.00</strong>
              <span>drift score</span>
            </div>
          </div>
        </div>

        <div class="proof-links">
          <a href="/benchmark">Explore PrecisionMemBench →</a>
          <a href="https://arxiv.org/abs/2605.11325">Read the memory paper →</a>
        </div>
      </div>
    </section>

    <section id="visibility">
      <div class="container">
        <span class="label">Built to be inspected</span>
        <h2>Nothing important should be invisible.</h2>
        <p class="section-lede">
          See what Tenure knew, what context the model received, and why an agent action was allowed or blocked. Track every change in real time; not after something goes wrong.
        </p>

        <div class="visibility-grid">
          <article class="audit-window">
            <div class="audit-window-bar">
              <span>MEMORY RECORD</span>
              <b>belief:v12</b>
            </div>
            <div class="audit-window-body">
              <span class="audit-field">TYPE <b>decision</b></span>
              <span class="audit-field">SCOPE <b>project:payments</b></span>
              <p>&ldquo;Payments migration targets PostgreSQL 18.&rdquo;</p>
              <div class="audit-meta">source: session_482 · supersedes: belief:v11</div>
            </div>
          </article>

          <article class="audit-window">
            <div class="audit-window-bar">
              <span>INJECTION LOG</span>
              <b>turn:1842</b>
            </div>
            <div class="audit-window-body">
              <span class="audit-field">TOKEN <b>agent:code-reviewer</b></span>
              <span class="audit-field">ELIGIBLE <b>3 beliefs</b></span>
              <p>Injected project decision + team API practice.</p>
              <div class="audit-meta">user persona excluded by token policy</div>
            </div>
          </article>

          <article class="audit-window">
            <div class="audit-window-bar">
              <span>RUNTIME DECISION</span>
              <b class="audit-red">BLOCK</b>
            </div>
            <div class="audit-window-body">
              <span class="audit-field">POLICY <b>#104 · v7</b></span>
              <span class="audit-field">EVIDENCE <b>active_account=true</b></span>
              <p>Follow-up offer blocked before CRM action executed.</p>
              <div class="audit-meta">decision and evidence recorded at runtime</div>
            </div>
          </article>
        </div>
      </div>
    </section>



    <section id="cta">
      <div class="container cta-card">
        <span class="label">Get started</span>
        <h2>Secure and contextualize every AI request at runtime.</h2>
        <p class="cta-sub">Run Tenure Community to give institutional knowledge to your AI tools, or leverage Tenure Team to govern every outbound LLM call across your entire company.</p>

        <div class="hero-cta-row">
         <a href="/pricing" class="btn-primary">View Pricing</a>
 <a href="/docs/quickstart" class="btn-secondary">Get Started</a>        
 </div>
      </div>
    </section>

  </main>

  <style>
    .hero-sub-lede {
      font-size: 1.15rem;
      line-height: 1.55;
    }

    .hero-stats-secondary {
      margin-top: 1.4rem;
    }

    .gateway-mock {
      max-width: 640px;
      margin: 1.6rem 0 0;
      border-radius: 14px;
      overflow: hidden;
      border: 1px solid var(--border2, #2a2a2a);
      background: var(--surface, #111111);
      box-shadow: 0 24px 60px -30px rgba(0,0,0,0.6);
      font-family: var(--mono);
      margin-left: auto;
      margin-top: 6rem;
      margin-right: auto;
    }

    .gateway-mock-bar {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.6rem 0.8rem;
      background: var(--surface2, #1a1a1a);
      border-bottom: 1px solid var(--border, #222222);
    }

    .gateway-mock-title {
      margin-left: 0.5rem;
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 0.04em;
      color: var(--muted, #b8b8b8);
      text-transform: uppercase;
    }

    .gateway-mock-agent {
      color: var(--accent, #00a8ad);
    }

    .gateway-mock-body {
      padding: 1rem 1.1rem 1.15rem;
      text-align: center;
    }

    .gateway-mock-intent {
      display: flex;
      flex-direction: column;
      gap: 0.2rem;
      margin-bottom: 0.85rem;
    }

    .gateway-mock-eyebrow {
      font-size: 0.65rem;
      font-weight: 700;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: var(--dim, #7a7a7a);
    }

    .gateway-mock-eyebrow-checks {
      margin-bottom: 0.5rem;
    }

    .gateway-mock-quote {
      font-size: 0.9rem;
      color: var(--text, #e8e8e8);
    }

    .gateway-checks {
      list-style: none;
      margin: 0 0 0.9rem;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 0.4rem;
    }

    .gateway-check {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 0.3rem 0.6rem;
      padding: 0.45rem 0.6rem;
      border-radius: 8px;
      border: 1px solid var(--border, #222222);
      background: rgba(255,255,255,0.02);
      font-size: 0.76rem;
      opacity: 0;
      animation: gatewayReveal 0.4s ease forwards;
      animation-delay: calc(var(--d) * 0.35s);
    }

    .gateway-check-name {
      flex: 1 1 auto;
      color: var(--text, #e8e8e8);
    }

    .gateway-check-dim {
      color: var(--dim, #7a7a7a);
    }

    .gateway-check-badge {
      font-weight: 800;
      font-size: 0.68rem;
      letter-spacing: 0.04em;
      padding: 0.1rem 0.45rem;
      border-radius: 999px;
    }

    .gateway-check-pass {
      border-color: rgba(34,197,94,0.25);
    }

    .gateway-check-pass .gateway-check-badge {
      color: var(--green, #22c55e);
      background: rgba(34,197,94,0.12);
      border: 1px solid rgba(34,197,94,0.3);
    }

    .gateway-check-fail {
      border-color: rgba(196,83,83,0.35);
      background: rgba(196,83,83,0.06);
    }

    .gateway-check-fail .gateway-check-badge {
      color: var(--red, #c45353);
      background: rgba(196,83,83,0.14);
      border: 1px solid rgba(196,83,83,0.35);
    }

    .gateway-check-evidence {
      flex-basis: 100%;
      color: var(--red, #c45353);
      opacity: 0.85;
      font-size: 0.7rem;
      padding-left: 1.2rem;
    }

    .gateway-decision {
      display: flex;
      flex-wrap: wrap;
      align-items: baseline;
      gap: 0.5rem;
      padding: 0.6rem 0.7rem;
      border-radius: 8px;
      border: 1px solid rgba(196,83,83,0.4);
      background: rgba(196,83,83,0.1);
      opacity: 0;
      animation: gatewayReveal 0.4s ease forwards;
      animation-delay: calc(var(--d) * 0.35s);
    }

    .gateway-decision-label {
      font-size: 0.65rem;
      font-weight: 700;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: var(--dim, #7a7a7a);
    }

    .gateway-decision-value {
      font-size: 0.85rem;
      font-weight: 800;
      color: var(--red, #c45353);
    }

    .gateway-decision-rule {
      margin-left: auto;
      font-size: 0.68rem;
      color: var(--dim, #7a7a7a);
    }

    @keyframes gatewayReveal {
      from { opacity: 0; transform: translateY(4px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 640px) {
      .gateway-mock-body {
        padding: 0.85rem 0.8rem 1rem;
      }

      .gateway-decision-rule {
        margin-left: 0;
      }
    }

    .architecture-section {
      position: relative;
      overflow: hidden;
    }

    .architecture-shell {
      max-width: 660px;
      margin: 2.5rem auto 0;
      padding: 0.85rem;
      border: 1px solid rgba(0, 168, 173, 0.22);
      border-radius: 20px;
      background:
        radial-gradient(circle at top, rgba(0, 168, 173, 0.18), transparent 34%),
        linear-gradient(180deg, rgba(255,255,255,0.045), rgba(255,255,255,0.015));
    }

    .architecture-row {
      display: grid;
      gap: 0.45rem;
    }

    .architecture-row-top {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .architecture-row-bottom-models {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .groundeval-strip {
      max-width: 660px;
      margin: 1.25rem auto 0;
      padding: 0.65rem 0.8rem;
      border: 1px dashed rgba(108, 92, 231, 0.4);
      border-radius: 14px;
      background: rgba(108, 92, 231, 0.05);
    }

    .groundeval-label {
      display: flex;
      align-items: baseline;
      gap: 0.6rem;
      margin-bottom: 0.7rem;
    }

    .groundeval-kicker {
      font-size: 0.72rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--violet, #a29bfe);
      padding: 0.2rem 0.55rem;
      border-radius: 999px;
      border: 1px solid rgba(108, 92, 231, 0.35);
      background: rgba(108, 92, 231, 0.14);
    }

    .groundeval-caption {
      font-size: 0.85rem;
      color: var(--dim, #7a7a7a);
    }

    .groundeval-steps {
      display: grid;
      grid-template-columns: 1fr auto 1fr auto 1fr;
      align-items: center;
      gap: 0.5rem;
    }

    .groundeval-step {
      padding: 0.45rem 0.55rem;
      border-radius: 10px;
      background: var(--violet, #6c5ce7);
      border: 1px solid rgba(108, 92, 231, 0.5);
      text-align: center;
    }

    .groundeval-step strong {
      display: block;
      font-size: 0.74rem;
      color: #ffffff;
      margin-bottom: 0.15rem;
    }

    .groundeval-step span {
      display: block;
      font-size: 0.64rem;
      color: rgba(255, 255, 255, 0.78);
    }

    .groundeval-arrow {
      color: rgba(255, 255, 255, 0.55);
      font-size: 1.1rem;
      text-align: center;
    }

    .groundeval-connector {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin: 0.15rem 0;
}

.groundeval-connector-line {
  position: relative;
  width: 1px;
  height: 20px;
  background: linear-gradient(180deg, rgba(108,92,231,0.15), rgba(108,92,231,0.9));
}

.groundeval-connector-line::after {
  content: "";
  position: absolute;
  left: -4px;
  top: 12px;
  width: 8px;
  height: 8px;
  border-right: 1px solid rgba(108,92,231,0.9);
  border-bottom: 1px solid rgba(108,92,231,0.9);
  transform: rotate(45deg);
}

.groundeval-connector-label {
  margin-top: 0.3rem;
  font-size: 0.78rem;
  font-weight: 700;
  color: var(--violet, #a29bfe);
  letter-spacing: 0.02em;
}

    .policy-control {
      border-color: rgba(108, 92, 231, 0.5);
      background: var(--violet, #6c5ce7);
    }

    .policy-control strong,
    .policy-control span {
      color: #ffffff;
    }

    .architecture-node {
      min-height: 34px;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 0.4rem;
      border-radius: 10px;
      font-weight: 700;
      font-size: 0.74rem;
      letter-spacing: -0.01em;
      color: var(--fg, #e8e8e8);
      border: 1px solid rgba(255,255,255,0.08);
      background: rgba(255,255,255,0.04);
    }

    .client-node {
      border-color: rgba(255,255,255,0.11);
    }

    .model-node {
      background: rgba(0,0,0,0.22);
      border-color: rgba(0,168,173,0.18);
      color: var(--dim, #a6a6a6);
    }

    .architecture-arrow-row {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 0.45rem;
      padding: 0.15rem 0;
    }

    .architecture-arrow-row-bottom {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .architecture-arrow-row span {
      height: 14px;
      position: relative;
    }

    .architecture-arrow-row span::before {
      content: "";
      position: absolute;
      left: 50%;
      top: 0;
      width: 1px;
      height: 8px;
      background: rgba(255,255,255,0.28);
    }

    .architecture-arrow-row span::after {
      content: "";
      position: absolute;
      left: calc(50% - 3px);
      top: 6px;
      width: 6px;
      height: 6px;
      border-right: 1px solid rgba(255,255,255,0.4);
      border-bottom: 1px solid rgba(255,255,255,0.4);
      transform: rotate(45deg);
    }

    .architecture-gateway {
      position: relative;
      padding: 1rem;
      border-radius: 18px;
      border: 1px solid rgba(0,168,173,0.4);
      background:
        linear-gradient(160deg, rgba(0,168,173,0.3), rgba(0,168,173,0.08)),
        #0b2e2b;
      box-shadow: inset 0 0 0 1px rgba(255,255,255,0.04);
    }

    .beta-badge {
      position: absolute;
      top: 0.7rem;
      right: 0.7rem;
      z-index: 2;
      padding: 0.25rem 0.6rem;
      border-radius: 999px;
      background: rgba(245,158,11,0.16);
      border: 1px solid rgba(245,158,11,0.4);
      color: #f5a623;
      font-size: 0.62rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    @media (max-width: 640px) {
      .beta-badge {
        top: 0.5rem;
        right: 0.5rem;
        font-size: 0.56rem;
        padding: 0.2rem 0.45rem;
      }
    }

    .architecture-gateway-header {
      max-width: 660px;
      margin: 0 auto 0.55rem;
      text-align: center;
    }

    .architecture-kicker {
      display: inline-flex;
      align-items: center;
      margin-bottom: 0.4rem;
      padding: 0.2rem 0.5rem;
      border-radius: 999px;
      background: rgba(0,168,173,0.16);
      border: 1px solid rgba(0,168,173,0.28);
      color: var(--accent, #00a8ad);
      font-size: 0.62rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    .architecture-gateway h3 {
      margin: 0;
      font-size: clamp(1.05rem, 2.2vw, 1.35rem);
      letter-spacing: -0.03em;
    }

    .architecture-gateway p {
      margin: 0.3rem auto 0;
      color: var(--dim, #9a9a9a);
      font-size: 0.76rem;
      line-height: 1.4;
    }

    .architecture-controls {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.5rem;
      margin-top: 0.6rem;
    }

    .architecture-controls-simple {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      max-width: 560px;
      margin-left: auto;
      margin-right: auto;
    }

    @media (max-width: 640px) {
      .architecture-controls-simple {
        grid-template-columns: 1fr;
      }
    }

    .architecture-control {
      text-align: center;
      min-height: 56px;
      padding: 0.55rem 0.65rem;
      border-radius: 12px;
      background: rgba(10,10,10,0.42);
      border: 1px solid rgba(255,255,255,0.08);
    }

    .architecture-control strong,
    .architecture-control span {
      display: block;
    }

    .architecture-control strong {
      margin-bottom: 0.8rem;
      color: var(--fg, #f0f0f0);
      font-size: 0.8rem;
    }

    .architecture-control span {
      color: var(--dim, #9a9a9a);
      font-size: 0.7rem;
      line-height: 1.3;
    }

    .context-lanes {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.35rem;
      margin-top: 0.6rem;
      padding-top: 0.6rem;
      border-top: 1px solid rgba(255,255,255,0.08);
    }

    .context-lanes span {
      padding: 0.25rem 0.5rem;
      border-radius: 999px;
      border: 1px solid rgba(0,168,173,0.22);
      background: rgba(0,168,173,0.08);
      color: var(--fg, #e8e8e8);
      font-size: 0.65rem;
      font-weight: 700;
    }

    .architecture-caption {
      max-width: 660px;
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.6rem;
      margin: 0.75rem auto 0;
    }

    .architecture-caption > div {
      padding: 0.6rem;
      border-radius: 12px;
      background: rgba(255,255,255,0.035);
      border: 1px solid rgba(255,255,255,0.07);
      color: var(--dim, #9a9a9a);
      font-size: 0.78rem;
      line-height: 1.4;
    }

    .architecture-caption strong {
      color: var(--fg, #eeeeee);
    }

    @media (max-width: 900px) {
      .architecture-row-top,
      .architecture-controls,
      .architecture-row-bottom-models,
      .architecture-caption {
        grid-template-columns: 1fr 1fr;
      }

      .architecture-arrow-row,
      .architecture-arrow-row-bottom {
        grid-template-columns: 1fr;
      }

      .architecture-arrow-row span:not(:first-child) {
        display: none;
      }
    }

    @media (max-width: 640px) {
      .architecture-shell {
        padding: 1rem;
        border-radius: 22px;
      }

      .architecture-row-top,
      .architecture-controls,
      .architecture-row-bottom-models,
      .architecture-caption {
        grid-template-columns: 1fr;
      }

      .groundeval-steps {
        grid-template-columns: 1fr;
      }

      .groundeval-arrow {
        transform: rotate(90deg);
      }
    }

    .wire-parse-mock {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1.5rem;
      max-width: 800px;
      margin: 4rem auto 0;
      font-family: var(--mono);
    }

    .wire-panel {
      flex: 1;
      background: #111111;
      border: 1px solid var(--border, #222222);
      border-radius: 12px;
      overflow: hidden;
    }

    .wire-panel.panel-highlight {
      border-color: rgba(0, 168, 173, 0.35);
      background: rgba(0, 168, 173, 0.02);
      padding-bottom: 0.8rem;
    }

    .wire-panel-header {
      font-size: 0.68rem;
      font-weight: 700;
      letter-spacing: 0.06em;
      background: #161616;
      padding: 0.6rem 0.9rem;
      border-bottom: 1px solid var(--border, #222222);
      color: var(--dim, #7a7a7a);
    }

    .wire-panel pre {
      margin: 0;
      padding: 1rem;
      font-size: 0.78rem;
      color: #e8e8e8;
      overflow-x: auto;
    }

    .wire-arrow-connector {
      font-size: 1.5rem;
      color: var(--border2, #2a2a2a);
    }

    .wire-matrix-row {
      display: flex;
      align-items: center;
      gap: 0.7rem;
      padding: 0.6rem 1rem;
      font-size: 0.8rem;
      border-bottom: 1px solid rgba(255,255,255,0.03);
      color: var(--fg, #e8e8e8);
    }

    .wire-matrix-row input[type="checkbox"] {
      width: 14px;
      height: 14px;
    }

    .wire-matrix-row code {
      font-size: 0.74rem;
      background: rgba(255,255,255,0.04);
      padding: 0.1rem 0.3rem;
      border-radius: 4px;
      color: var(--accent, #00a8ad);
    }

    .wire-caption {
      font-size: 0.68rem;
      color: var(--dim, #7a7a7a);
      padding: 0.8rem 1rem 0;
      line-height: 1.3;
    }

    @media (max-width: 768px) {
      .wire-parse-mock {
        flex-direction: column;
        align-items: stretch;
        gap: 1rem;
      }
      .wire-arrow-connector {
        transform: rotate(90deg);
        text-align: center;
        margin: 0.2rem 0;
      }
    }

    code {
  font-family: var(--mono);
  color: var(--accent);
}
  </style>

</Base>